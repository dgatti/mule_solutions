#Common App Properties
http:
  host: "0.0.0.0"
  port: "8081"
https:
  host: "0.0.0.0"
  port: "8082"
  private:
    port: "8092"

api:
  groupId:      "${api.groupId}"
  artifactId:   "${api.artifactId}"
  version:      "${api.version}"
  spec:         ""
  majorVersion: "v1"
  
error:
  domain: "${api.artifactId}"
  
json:
  logger:
    application:
      name:     "${api.artifactId}"
      version:  "${api.version}"
      
target:
  account:
    objectName: "Account"
    externalId: "External_Id__c"
    
  contentVersion:
    objectName: "S3_Document__c"
    id: ContentVErsionId__c
    
  case:
    objectName: "Case"
    notification: "true"
    externalId: "Id"
    
query:
  timeout: "10000"