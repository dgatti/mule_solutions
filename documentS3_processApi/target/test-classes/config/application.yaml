#Common App Properties
http:
  host: "0.0.0.0"
  port: "8081"
https:
  host: "0.0.0.0"
  port: "8082"

api:
  groupId:      "${api.groupId}"
  artifactId:   "${api.artifactId}"
  version:      "${api.version}"
  spec:         ""
  majorVersion: "v1"
  
json:
  logger:
    application:
      name:     "vems-mule-sfcore-sapi"
      version:  "1.0.0"
      
target:
  account:
    objectName: "Account"
    externalId: "Account_Id__c"
    
until:
  MaxRetries: "3"
  MsBwRetries: "1000"