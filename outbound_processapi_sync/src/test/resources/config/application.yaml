#Common App Properties
http:
  host: "0.0.0.0"
  port: "8081"
https:
  host: "0.0.0.0"
  port: "8082"

api:
  groupId:      "${api.groupId}"
  artifactId:   "${api.artifactId}"
  version:      "${api.version}"
  spec:         ""
  majorVersion: "v1"
  
json:
  logger:
    application:
      name:     "vems-mule-outbound-papi"
      version:  "1.0.0"
